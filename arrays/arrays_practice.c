#include <stdio.h>

int main(void) {
    int arr[10], i;

    for (i = 0; i < 10; i++) {
        printf("> ");
        scanf("%d", &arr[10]);
    }

    for (i = 0; i < 10; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}